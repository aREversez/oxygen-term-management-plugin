# Term Management 插件使用指南

这是一份面向**日常使用者**的操作说明，不涉及开发和源码内容。如果你是想了解插件内部实现或参与开发，请参考仓库里的 `README.md`。

---

## 一、安装

1. 找到已经打包好的插件文件（压缩包或者文件夹，通常叫 `term-management`）。
2. 把整个文件夹复制到 Oxygen 安装目录下的 `plugins` 文件夹里（比如 `C:\Program Files\Oxygen XML Editor 28\plugins\term-management`）。
3. **完全关闭并重新打开 Oxygen**（不是关闭文档，是整个软件退出重启）。
4. 重启后，在菜单栏找 `Window → Show View`，应该能找到一个叫 **Term Management** 的面板选项，点开它，插件就装好了。

---

## 二、界面总览

打开 Term Management 面板后，你会看到三个标签页：

- **Term Recognition（术语识别）**：扫描当前打开的文档，把文档里出现的术语标出来。
- **Terminology Management（术语管理）**：查看、新增、编辑、删除某一个术语库里的词条。
- **Termbase Search（术语库搜索）**：跨所有已启用的术语库，模糊搜索某个词。

先看第三步——怎么把术语库加进插件里，这是使用其他功能的前提。

---

## 三、第一步：添加术语库

1. 打开 Oxygen 的 `Options → Preferences`，在左侧列表里找到 **Term Management**。
2. 点击 **Add**，选择一个术语库文件——支持 **CSV**、**XLSX**、**TBX** 三种格式。
3. 添加后，列表里会显示这个术语库，前面有个勾选框，**勾上表示启用**。可以同时启用多个术语库。
4. 如果某个术语库暂时不想用了，取消勾选就行，不用删除。
5. 术语库文件本身如果被更新了（比如同事改了内容），点击 **Reload** 重新加载，不用重启 Oxygen。

---

## 四、日常使用场景

### 场景 1：我想知道这篇文档里出现了哪些术语

1. 打开 **Term Recognition** 标签页。
2. 顶部下拉框选择要用哪个术语库对照（如果只启用了一个，会自动选中）。
3. 点击 **Scan** 按钮，下方会列出文档里匹配到的所有术语，以及各自出现了几次。
4. 如果想在文档里直接看到高亮标记，点一下 **Toggle Highlight** 按钮，匹配到的术语会在文档里以背景色标出来，方便直接在正文里找。
5. 结果列表里双击某一行，会自动跳转到 **Terminology Management** 标签页，并且定位到这个术语，方便直接编辑。
6. 用 **< 上一个** / **下一个 >** 按钮，可以在文档里逐个跳转到每一处术语出现的位置。

### 场景 2：我在写文档，选中一个词，想快速处理

这是最常用的功能，不用来回切标签页。**在文档里选中一段文字，点右键**，会看到一个 **Term Management** 的子菜单，里面根据你选中的内容，自动显示合适的选项：

| 你选中的内容 | 会看到什么选项 | 干什么用 |
|---|---|---|
| 一个术语库里**没有**的新词 | **Quick Add** | 弹出一个小窗口，让你填写对应的译法，确认后直接存进术语库 |
| 一个术语库里**已经有**的词 | **Insert Translation** | 直接把这个词换成对应的译文，插入到文档里 |
| 一个术语库里**已经有**的词 | **Edit Term** | 弹出编辑窗口，直接修改这个词条的内容 |
| 任何选中的文字 | **Search in Termbase** | 跳到搜索标签页，自动帮你搜一遍所有术语库 |

**几个需要注意的小细节：**

- 右键菜单用的是 **Term Recognition 标签页里当前选中的那个术语库**，不是"所有启用的术语库"。如果发现右键菜单查不到你以为应该存在的词，先去 Term Recognition 那里确认一下选的是哪个术语库。
- 如果你选中的一段文字里，**同时包含了两个不同的术语**（比如"应力集中和疲劳强度"这种一次选了两个词的情况），右键菜单只会显示一条提示，不会出现任何可点击的选项——这是有意设计成这样的，插件没法判断你到底想处理哪一个，所以干脆不猜，你需要每次只选中一个术语再操作。
- 用鼠标 Ctrl+ 点选中文档里几处不连续的位置时，右键不会出现 Term Management 菜单，这也是正常的，不是插件坏了。

### 场景 3：我想手动整理术语库（新增/改错字/删掉旧词）

打开 **Terminology Management** 标签页：

- **新增一条**：点 **Quick Add** 按钮（如果当前编辑器里有选中文字，会自动带过来当源语），或者用旁边的"新增"按钮从空白开始填。
- **改一处内容**：直接在表格里双击对应的单元格，改完按回车，会自动保存，不需要额外点保存按钮。
- **删除**：选中一行或者多行（可以按住 Ctrl 多选），点删除，会有确认提示，写明"要删除几条"。
- **删错了想反悔**：删除之后，马上能用 **Undo** 撤销上一次删除操作（只能撤销最近一次）。
- **术语库很大，想快速定位**：表格上方有个筛选输入框，输入关键字实时过滤，不会修改原始文件顺序;点 **Reset Sort** 可以恢复到没筛选、没排序的原始状态。

**如果新增的词，源语在术语库里已经存在**，插件会弹出提示，告诉你"是同一个译法还是不同的译法"，你可以选择是否仍要继续添加——这是为了避免不小心加进去重复或者相互矛盾的翻译。

### 场景 4：我不确定某个词该怎么翻，想查一下

打开 **Termbase Search** 标签页，输入关键词（源语或者目标语都可以搜），会**跨所有已启用的术语库**一起搜，不局限于某一个。双击搜索结果，同样会跳转到 Terminology Management 标签页方便你编辑。

---

## 五、界面语言

插件的界面语言跟着 **Oxygen 本身设置的界面语言**走，不是跟电脑系统语言走。目前支持中文、英文、法语、德语、日语。如果想切换，去 Oxygen 的 `Options → Preferences → Global` 里改界面语言，**改完需要重启 Oxygen 才会生效**。

---

## 六、常见问题

**Q：我改了术语库文件（比如用 Excel 直接改的），插件里怎么没更新？**
A：去 Preferences 里点一下对应术语库的 **Reload** 按钮，或者重启 Oxygen。

**Q：保存术语的时候提示"文件被占用"或者类似的报错？**
A：说明这个术语库文件正被别的程序打开（最常见的是 Excel 打开着对应的 XLSX 文件），先把那边关掉再重试。

**Q：右键点了 Quick Add / Search in Termbase，但是什么反应都没有？**
A：先确认一下你是否打开过 Term Management 面板（哪怕只是打开看一眼再切走也算），插件的某些功能需要面板至少被打开初始化过一次才能正常响应右键操作。

**Q：高亮显示的颜色跟 Oxygen 的深色/浅色主题不搭？**
A：图标和部分界面元素会自动跟随 Oxygen 的主题切换，如果切换主题后显示还是不对，尝试重新打开一下插件面板。

---

如果遇到这份指南没覆盖到的问题，或者操作起来跟这里描述的不一致，可以直接反馈给维护者（仓库地址见 `README.md`）。