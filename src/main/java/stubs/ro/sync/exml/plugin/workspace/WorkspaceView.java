package ro.sync.exml.plugin.workspace;

import javax.swing.JInternalFrame;

/**
 * Stub class for Oxygen XML Editor WorkspaceView.
 * Used for compilation only.
 */
public class WorkspaceView extends JInternalFrame {
    public WorkspaceView(String title, boolean resizable, boolean closable, boolean maximizable, boolean iconifiable) {
        super(title, resizable, closable, maximizable, iconifiable);
    }

    public void setVisible(boolean visible) { super.setVisible(visible); }
    public void setSize(int width, int height) { super.setSize(width, height); }
}
